server:
  port: 8083

spring:
  application:
    name: sbi-imps-application
  datasource:
    url: jdbc:mysql://localhost:3306/gc2
    username: root
    password: Mysql@2022
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  activemq:
    broker-url: tcp://localhost:61616
    user: admin
    password: admin
